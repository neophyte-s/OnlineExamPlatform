class Question{
    constructor(id, name, options,score){
         this.id = id;
         this.name = name;
         this.options = options;
         this.sans = 0;
         this.score = score;   
    }
}